import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { createClient } from '@supabase/supabase-js';
import './style.css';

const supabase = createClient(import.meta.env.VITE_SUPABASE_URL, import.meta.env.VITE_SUPABASE_ANON_KEY);

function Header(){return <header><div className="logo">V&A</div><h1>Vincos & Agulha</h1><p>Gestão de produção para engomadoria</p></header>}
function Login({onLogin}){const[code,setCode]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');async function submit(){setError('');const{data,error}=await supabase.rpc('login_user',{p_code:code,p_password:password});if(error){setError(error.message);return}if(!data||data.length===0){setError('Código ou senha incorretos.');return}onLogin(data[0])}return <section className="card login"><h2>Entrar</h2><label>Código de acesso</label><input value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="Ex: GER001 ou F001"/><label>Senha</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha"/><button onClick={submit}>Entrar</button><p className="muted">Acesso inicial: <b>GER001</b> / <b>123456</b></p>{error&&<div className="warning">{error}</div>}</section>}
function ChangePassword({user,onDone}){const[p1,setP1]=useState('');const[p2,setP2]=useState('');const[error,setError]=useState('');async function submit(){setError('');if(p1.length<4){setError('A senha deve ter pelo menos 4 caracteres.');return}if(p1!==p2){setError('As senhas não coincidem.');return}const{error}=await supabase.rpc('change_password',{p_user_id:user.id,p_new_password:p1});if(error){setError(error.message);return}onDone({...user,must_change_password:false})}return <section className="card login"><h2>Alterar senha</h2><p><b>Bem-vindo, {user.name}.</b></p><div className="warning">Por segurança, altere a senha no primeiro acesso.</div><label>Nova senha</label><input type="password" value={p1} onChange={e=>setP1(e.target.value)}/><label>Confirmar senha</label><input type="password" value={p2} onChange={e=>setP2(e.target.value)}/><button onClick={submit}>Guardar nova senha</button>{error&&<div className="warning">{error}</div>}</section>}
function App(){const[user,setUser]=useState(null);const[tab,setTab]=useState('registar');const[clients,setClients]=useState([]);const[users,setUsers]=useState([]);const[records,setRecords]=useState([]);const[notice,setNotice]=useState('');async function load(){const[c,u,r]=await Promise.all([supabase.from('clients').select('*').order('name'),supabase.from('app_users').select('id,name,access_code,role,active,must_change_password,created_at').order('created_at',{ascending:false}),supabase.from('production_records').select('*').order('created_at',{ascending:false})]);setClients(c.data||[]);setUsers(u.data||[]);setRecords(r.data||[])}useEffect(()=>{if(user)load()},[user]);if(!user)return <><Header/><main><Login onLogin={setUser}/></main></>;if(user.must_change_password)return <><Header/><main><ChangePassword user={user} onDone={setUser}/></main></>;const manager=user.role==='gerente';return <><Header/><main><section className="card"><b>Sessão:</b> {user.name} | <b>Código:</b> {user.access_code} | <b>Perfil:</b> {user.role}<button className="danger" onClick={()=>setUser(null)} style={{marginTop:10}}>Sair</button></section><div className="tabs"><button onClick={()=>setTab('registar')}>Registar Peças</button><button onClick={()=>setTab('dashboard')}>Dashboard</button>{manager&&<button onClick={()=>setTab('clientes')}>Clientes</button>}{manager&&<button onClick={()=>setTab('funcionarios')}>Funcionários</button>}<button onClick={()=>setTab('historico')}>Histórico</button></div>{tab==='registar'&&<Register user={user} clients={clients} load={load} notice={notice} setNotice={setNotice}/>} {tab==='dashboard'&&<Dashboard clients={clients} records={records}/>} {tab==='clientes'&&manager&&<Clients clients={clients} load={load}/>} {tab==='funcionarios'&&manager&&<Users users={users} manager={user} load={load}/>} {tab==='historico'&&<History records={records}/>}<div className="footer">Vincos & Agulha — Aplicação Online V1.0</div></main></>}
function Register({user,clients,load,notice,setNotice}){const[clientId,setClientId]=useState('');const[pieces,setPieces]=useState('');const[notes,setNotes]=useState('');async function save(){setNotice('');const client=clients.find(c=>c.id===clientId);if(!client){setNotice('Selecione o cliente.');return}if(!Number(pieces)||Number(pieces)<1){setNotice('Indique um número válido de peças.');return}const{error}=await supabase.from('production_records').insert({client_id:client.id,client_name:client.name,user_id:user.id,user_name:user.name,user_code:user.access_code,pieces:Number(pieces),notes});if(error){setNotice(error.message);return}setPieces('');setNotes('');setNotice('Registo guardado com sucesso.');load()}return <section className="card"><h2>Registar peças passadas</h2><p><b>Funcionário:</b> {user.name}</p><label>Cliente</label><select value={clientId} onChange={e=>setClientId(e.target.value)}><option value="">Selecionar cliente</option>{clients.filter(c=>c.active).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><label>Número de peças</label><input type="number" min="1" value={pieces} onChange={e=>setPieces(e.target.value)} placeholder="Ex: 125"/><label>Observações</label><textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Opcional"/><button onClick={save}>Guardar</button>{notice&&<div className={notice.includes('sucesso')?'success':'warning'}>{notice}</div>}</section>}
function Dashboard({clients,records}){const today=new Date().toISOString().slice(0,10);const month=new Date().toISOString().slice(0,7);const totalToday=records.filter(r=>r.created_at.slice(0,10)===today).reduce((s,r)=>s+r.pieces,0);const totalMonth=records.filter(r=>r.created_at.slice(0,7)===month).reduce((s,r)=>s+r.pieces,0);const total=records.reduce((s,r)=>s+r.pieces,0);const byClient={},byUser={};records.forEach(r=>{byClient[r.client_name]=(byClient[r.client_name]||0)+r.pieces;byUser[r.user_name]=(byUser[r.user_name]||0)+r.pieces});return <section className="card"><h2>Dashboard</h2><div className="grid"><Stat title="Peças hoje" value={totalToday}/><Stat title="Peças no mês" value={totalMonth}/><Stat title="Total geral" value={total}/><Stat title="Clientes ativos" value={clients.filter(c=>c.active).length}/></div><h3>Produção por cliente</h3><Table rows={Object.entries(byClient).sort((a,b)=>b[1]-a[1])}/><h3>Produção por funcionário</h3><Table rows={Object.entries(byUser).sort((a,b)=>b[1]-a[1])}/></section>}
function Stat({title,value}){return <div className="card"><div className="muted">{title}</div><div className="stat">{value}</div></div>}
function Table({rows}){return <div className="scroll"><table><tbody>{rows.map(([a,b])=><tr key={a}><td>{a}</td><td>{b}</td></tr>)}</tbody></table></div>}
function Clients({clients,load}){const[form,setForm]=useState({name:'',phone:'',address:'',notes:''});async function create(){if(!form.name)return alert('Indique o nome do cliente.');const{error}=await supabase.from('clients').insert(form);if(error)return alert(error.message);setForm({name:'',phone:'',address:'',notes:''});load()}async function toggle(id){const{error}=await supabase.rpc('toggle_client_active',{p_client_id:id});if(error)return alert(error.message);load()}return <section className="card"><h2>Gestão de clientes</h2><div className="row"><div><label>Nome</label><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label>Telefone</label><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div></div><label>Morada</label><input value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><label>Observações</label><textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/><button onClick={create}>Criar cliente</button><div className="scroll"><table><thead><tr><th>Cliente</th><th>Telefone</th><th>Morada</th><th>Estado</th><th>Ações</th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><td>{c.name}</td><td>{c.phone}</td><td>{c.address}</td><td>{c.active?'Ativo':'Inativo'}</td><td><button className="small-btn secondary" onClick={()=>toggle(c.id)}>{c.active?'Inativar':'Ativar'}</button></td></tr>)}</tbody></table></div></section>}
function Users({users,manager,load}){const[form,setForm]=useState({name:'',code:'',password:'',role:'funcionario'});async function create(){if(!form.name||!form.code||!form.password)return alert('Preencha nome, código e senha inicial.');const{error}=await supabase.rpc('create_app_user',{p_manager_id:manager.id,p_name:form.name,p_code:form.code,p_temp_password:form.password,p_role:form.role});if(error)return alert(error.message);setForm({name:'',code:'',password:'',role:'funcionario'});load()}async function reset(id){const p=prompt('Nova senha temporária:');if(!p)return;const{error}=await supabase.rpc('reset_user_password',{p_manager_id:manager.id,p_user_id:id,p_temp_password:p});if(error)return alert(error.message);alert('Senha reposta. O funcionário terá de alterar no próximo acesso.');load()}async function toggle(id){const{error}=await supabase.rpc('toggle_user_active',{p_manager_id:manager.id,p_user_id:id});if(error)return alert(error.message);load()}return <section className="card"><h2>Gestão de funcionários</h2><div className="row"><div><label>Nome</label><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label>Código</label><input value={form.code} onChange={e=>setForm({...form,code:e.target.value.toUpperCase()})}/></div></div><div className="row"><div><label>Senha inicial</label><input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><div><label>Perfil</label><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="funcionario">Funcionário</option><option value="gerente">Gerente</option></select></div></div><button onClick={create}>Criar funcionário</button><div className="scroll"><table><thead><tr><th>Código</th><th>Nome</th><th>Perfil</th><th>Ativo</th><th>Primeiro acesso</th><th>Ações</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.access_code}</td><td>{u.name}</td><td>{u.role}</td><td>{u.active?'Sim':'Não'}</td><td>{u.must_change_password?'Sim':'Não'}</td><td><button className="small-btn secondary" onClick={()=>reset(u.id)}>Repor senha</button>{u.access_code!=='GER001'&&<button className="small-btn secondary" onClick={()=>toggle(u.id)}>{u.active?'Inativar':'Ativar'}</button>}</td></tr>)}</tbody></table></div></section>}
function History({records}){function exportCSV(){const lines=[['Data','Cliente','Funcionario','Codigo','Pecas','Observacoes']];records.forEach(r=>lines.push([new Date(r.created_at).toLocaleString('pt-PT'),r.client_name,r.user_name,r.user_code,r.pieces,r.notes||'']));const csv=lines.map(l=>l.map(v=>`"${String(v).replaceAll('"','""')}"`).join(';')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8;'}));a.download='vincos_agulha_registos.csv';a.click()}return <section className="card"><h2>Histórico</h2><button onClick={exportCSV}>Exportar CSV/Excel</button><div className="scroll"><table><thead><tr><th>Data</th><th>Cliente</th><th>Funcionário</th><th>Código</th><th>Peças</th><th>Observações</th></tr></thead><tbody>{records.map(r=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString('pt-PT')}</td><td>{r.client_name}</td><td>{r.user_name}</td><td>{r.user_code}</td><td>{r.pieces}</td><td>{r.notes}</td></tr>)}</tbody></table></div></section>}

createRoot(document.getElementById('root')).render(<App/>);